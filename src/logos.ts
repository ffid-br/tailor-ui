import claro from './assets/tailor-logo.png';
import branco from './assets/tailor-logo-branco.png';

/** Wordmark Tailor.ia para fundo claro (data URI embutido no pacote). */
export const logoTailor: string = claro;
/** Wordmark Tailor.ia branco, para fundo escuro (data URI embutido no pacote). */
export const logoTailorBranco: string = branco;
